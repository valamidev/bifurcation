export declare class bifurcation {
    input: string;
    result: number;
    protected age: number;
    protected coefficient: number;
    constructor(coefficient: number, modular: number);
    update(): void;
}
